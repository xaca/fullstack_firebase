{
  "name": "fullstack",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "clean:dev": "rimraf ./dist",
    "clean:prod": "rimraf ./public",
    "dev": "npm run clean:dev && parcel ./src/*.html",
    "build": "npm run clean:prod && parcel build ./src/*.html --dist-dir ./public"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^2.9.2",
    "process": "^0.11.10",
    "rimraf": "^5.0.1"
  },
  "dependencies": {
    "firebase": "^9.14.0"
  }
}
